import { PlayerDirective } from './player.directive';

describe('PlayerDirective', () => {
  it('should create an instance', () => {
    const directive = new PlayerDirective();
    expect(directive).toBeTruthy();
  });
});
